# Vibecode Protocol Spec

## Base

`vibecode://run`

## Params

| Param | Description |
| --- | --- |
| tool | `codex` / `claude` / `vscode` / `continue` |
| prompt | URL-encoded prompt |
| repo | Optional repo path |

## Example

`vibecode://run?tool=codex&prompt=Fix+tests`
